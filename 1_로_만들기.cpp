#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> num;
    vector<int> result;
    int n;
    cin >> n;
    result.resize(1000001);

    //백만까지의 배열
    for(int i = 1; i <= 1000000; i++) num.push_back(i);
    
    if(true);
    cout << result[n];
    return 0;
}