#include <iostream>
#include <algorithm>
#include <map>
#include <vector>
using namespace std;

int main()
{
    int entry;
    cin >> entry;
    vector<vector<int>> scoreboard;
    for(int i = 0; i < entry; i++)
    {
        int contry, student, score;
        cin >> contry >> student >> score;
    }
    return 0;
}