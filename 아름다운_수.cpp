#include <iostream>

using namespace std;

int main()
{
    int t, num, result = 0;
    string str;
    cin >> t;
    for(int i = 0; i < t; i++)
    {
        cin >> num;
        str = to_string(num);

        for(int j = 0; j < str.size(); j++)
        {
            
        }
    }

    return 0;
}